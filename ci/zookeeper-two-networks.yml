- type: replace
  path: /instance_groups/name=zookeeper/networks/-
  value:
    name: ipv6-only
    default: [dns, gateway]

- type: replace
  path: /instance_groups/name=smoke-tests/networks/-
  value:
    name: ipv6-only
    default: [dns, gateway]
